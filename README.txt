This program requires python 3.8+ with the requests and bs4 packages
Once python is installed these packages can be installed with the commands

pip install requests
pip install bs4

The program will prompt the user for an album ID and display the
contents of that album.
It then asks the user if they would like to display another album
and the user can enter yes or no.